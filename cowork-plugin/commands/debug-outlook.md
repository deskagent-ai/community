# Agent: Debug Outlook

Prüfe die Outlook-Verbindung und zeige:

1. Rufe `outlook_list_folders()` auf um alle Postfächer und Ordner zu sehen
2. Rufe `outlook_get_folder_emails("ToOffer", limit=5)` auf
3. Rufe `outlook_get_flagged_emails(limit=10)` auf

Zeige die RAW JSON Antworten - keine Interpretation, nur die Daten!
